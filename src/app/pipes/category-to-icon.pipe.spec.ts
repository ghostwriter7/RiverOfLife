import { CategoryToIconPipe } from './category-to-icon.pipe';

describe('CategoryToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
